# QA 프론트 시나리오 감축 로그 (2026-07-08)

원본 4축 패턴 카탈로그를 모든 화면에 기계적으로 적용해 도출된 **422개**를, 수동 QA 1회전에 현실적인 **144개**로 감축했다.

- 원본: `QA_프론트_시나리오_2026-06-29.csv` (422개)
- 정리본: `QA_프론트_시나리오_slim_2026-07-08.csv` (144개, UTF-8 BOM)

## 결과 요약

| 구분 | 원본 | 유지 | 제거 |
|---|---|---|---|
| P0 | 80 | **80 (전량)** | 0 |
| P1 | 173 | 64 | 109 |
| P2 | 169 | 0 | 169 |
| **합** | **422** | **144** | **278** |

| 모듈 | 원본→유지 | 모듈 | 원본→유지 |
|---|---|---|---|
| 온보딩·권한 | 36→14 | 지도 | 24→10 |
| 스플래시 | 14→7 | 관심 | 40→13 |
| 메인탭 | 28→11 | 마이페이지 | 42→13 |
| 경매홈 | 58→20 | 설정 | 46→14 |
| 경매상세 | 65→22 | 댓글 | 2→1 |
| 검색 | 61→18 | 신고 | 6→1 |

## 감축 원칙

1. **P0 전량 유지** — 핵심 동선·크래시·진입 불가급은 하나도 버리지 않음.
2. **P2 전량 보류(169개)** — 엣지/부가 케이스. 정리본에서 제외하되 원본에 보존되어 있으므로 여유가 생기면 개별 복원.
3. **P1은 화면 대표 케이스만 유지, 모듈 간 반복은 제거** — 아래 패턴 참조.

## 걷어낸 반복 패턴 (모듈 횡단 중복)

| 패턴 | 처리 | 근거 |
|---|---|---|
| **다크모드** (~13개) | **전량 제거** | 앱 다크모드 공식 지원 미정. 다수 시나리오에 "지원 여부 확인 필요" 주석 → 추측성 |
| **백그라운드→포그라운드 복귀** (~9개) | 전량 제거 | 모듈마다 "상태 유지/크래시 없음"으로 동일. 대표 동선은 각 화면 상태보존 P0/P1에 흡수 |
| **재진입/탭 복귀 상태 보존** (~10개) | 대부분 제거 | 의미 있는 것(경매홈 필터 보존 등)만 유지 |
| **긴 텍스트/오버플로우** (~8개) | 전량 제거(P2) | 셀 레이아웃 반복. 필요 시 회귀로 흡수 |
| **이미지 로딩 플레이스홀더** (AH/IN/MY/SC) | 전량 제거(P2) | 동일 `CachedAsyncImage` 컴포넌트 |
| **찜(관심) 토글 5종** (ON/OFF/실패/동기화/중복탭) | **경매상세(AD)에 정본 유지**, 타 화면은 대표 토글+동기화만 | 공용 로직. AD-030/031/032/033 + 동기화가 대표 |
| **공용 필터 바텀시트** (건물용도/경매상태/가격) | **경매홈(AH)에 정본 유지**(건물용도 다중선택+디바운스로 대표), 검색은 "시트 열림+dismiss 재로드"만 | 경매홈·검색이 동일 컴포넌트 공유 |
| **공용 정렬 바텀시트** | 경매홈 유지, 검색은 열기/선택만 | 동일 |
| **중복 탭/디바운스 (일반)** | 대부분 제거 | 크래시·중복 push 위험 있는 P0 동선만 유지 |

## 미구현 모듈 축약

- **댓글(CM)**: 2개 → **1개**. Tuist 미구현 스캐폴드(`tmp.swift` 빈 파일). `FE-CM-002`(구현 여부 확인, QA 불가 상태)만 유지. 기능 구현 후 재작성 필요.
- **신고(RP)**: 6개 → **1개**. 플레이스홀더 화면(`ReportView` 텍스트만). `FE-RP-002`(진입 시 크래시/프리즈 없음, P0)만 유지. 실제 신고 UI 구현 후 확장.

## 복원 방법

정리본에서 빠진 시나리오가 필요하면 원본 CSV(`QA_프론트_시나리오_2026-06-29.csv`)에서 해당 `FE-*` ID 행을 그대로 복사해 정리본에 붙이면 된다. 정리본은 원본의 부분집합이며 컬럼 스키마가 동일하다.

## 후속 권장

- 2차 회귀에서 P2 중 **미구현/잠재 결함 주석이 달린 케이스**(찜 실패 시 알럿 부재, 최근 본 매물 에러 UI 부재, 약관 웹뷰 에러 핸들링 부재 등)는 기획/개발 확인용으로 별도 트래킹 권장.
- 다크모드 지원이 확정되면 앱 전체 다크모드 스모크 1건을 신설(모듈별 개별 케이스 부활 대신).
